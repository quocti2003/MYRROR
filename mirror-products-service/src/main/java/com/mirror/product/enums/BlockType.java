package com.mirror.product.enums;

/**
 * Enum representing types of blocked slots
 */
public enum BlockType {
    SINGLE,           // Single occurrence block
    RECURRING_DAILY,  // Repeats every day
    RECURRING_WEEKLY  // Repeats on same day each week
}

package com.mirror.product.enums;

public enum VendorType {
    PARTS_ONLY,        // Delivers goods in parts only
    WHOLE_PIECE_ONLY,  // Delivers whole pieces only
    BOTH             // Trial/new vendor
}